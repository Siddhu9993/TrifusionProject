version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - JWT_SECRET=supersecret123
      - DATABASE_URL=file:./dev.db
    volumes:
      - ./prisma:/app/prisma
